/*Ya importamos en el plugin validate en nuestro html*/

$(document).ready(function() {
	//Accedemos a nuestro formulario a traves de su id y le aplicamos la funcion validate de nuestro plugin
	$("#form_registro").validate();


});